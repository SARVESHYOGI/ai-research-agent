[project]
name = "llm-gateway"
version = "0.1.0"
description = "LLM provider abstraction: routing, retries, fallback, token and cost tracking."
requires-python = ">=3.12"
dependencies = [
    "shared",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "redis>=6",
    "pydantic-settings>=2",
    "openai>=1.40",
    "anthropic>=0.34",
    "tenacity>=9",
]

[tool.uv]
package = false