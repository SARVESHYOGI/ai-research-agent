[project]
name = "api-gateway"
version = "0.1.0"
description = "Public API gateway: auth, rate limiting, request validation and routing."
requires-python = ">=3.12"
dependencies = [
    "shared",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "redis>=6",
    "pydantic-settings>=2",
    "pyjwt>=2",
    "httpx>=0.27",
]

[tool.uv]
package = false