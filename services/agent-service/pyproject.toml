[project]
name = "agent-service"
version = "0.1.0"
description = "Orchestration brain: planner, state machine, context compiler, recovery."
requires-python = ">=3.12"
dependencies = [
    "shared",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "redis>=6",
    "pydantic-settings>=2",
    "psycopg[binary]>=3.2",
    "httpx>=0.27",
    "tenacity>=9",
]

[tool.uv]
package = false